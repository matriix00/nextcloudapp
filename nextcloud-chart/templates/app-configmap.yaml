apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-{{.Values.configname}} 
data:
{{- with .Values.data }}
  user: {{.user | quote}}
  db: {{.db | quote}}
  redishost: {{.redishost | quote}}
  postdb: {{.postdb | quote}}
  posthost: {{.posthost | quote}}
{{- end }}